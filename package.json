{
  "name": "agente-administrativos-conversacional-frontend",
  "version": "1.0.0",
  "description": "",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server/server.js",
    "start:dev": "nodemon server/server.js",
    "deploy:dev": "gcloud run deploy agente-administrativos-conversacional-frontend --source . --project umng-agentes-dev --region=us-central1 --allow-unauthenticated --service-account=sa-frontend-service@umng-agentes-dev.iam.gserviceaccount.com --env-vars-file=env.dev.yaml --labels=env=dev,source=agente-conversacional,component=frontend,domain=agente-administrativos,layer=final"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "module",
  "dependencies": {
    "express": "^5.2.1",
    "google-auth-library": "^10.5.0",
    "node-fetch": "^3.3.2"
  },
  "devDependencies": {
    "env-yaml": "^0.1.2",
    "nodemon": "^3.1.11"
  }
}
